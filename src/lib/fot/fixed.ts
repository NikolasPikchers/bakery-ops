/**
 * Фиксированные ежемесячные выплаты ФОТ, не привязанные к табелю/сменам/премиям.
 * Начисляются ровно `monthly` за полный месяц; на дашборде (ФОТ «по текущую дату»)
 * — пропорционально числу прошедших дней месяца. Управляются здесь, кодом
 * (как и штат — через seed-скрипт), отдельной БД-сущности для них нет.
 */
export type FixedSalary = { name: string; monthly: number };

export const FIXED_SALARIES: FixedSalary[] = [
  // Папа подрабатывает водителем-доставщиком каждый день, 30 000 ₽/мес.
  { name: 'Водитель (доставка)', monthly: 30000 },
];
